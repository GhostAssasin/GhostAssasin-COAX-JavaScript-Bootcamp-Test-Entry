const config = {
  delay: 10, // time before screensaver start (seconds);
  interval: 5, // time between changes of image (seconds);
  resize: true // resize image to maximum available size, if image size is bigger than screen (false/true);
}
